# 改行コードをLFに固定する(理由と経緯: docs/lint-policy.md「改行コード」)。
# 各環境の core.autocrlf の値によらず、indexと**新しくcloneした**作業ツリーがLFになる。
# 既にあるチェックアウトは自動では書き換わらない(移行手順は上記docの「既存のチェックアウトを移行する」)。
# バイナリは拡張子で列挙せず text=auto の自動判定に任せる(列挙はその一覧が検出の上限になる)。
* text=auto eol=lf
